{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cat_vulnerabilidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_Vulnerabilidad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cat_Vulnerabilidad.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cat_vulnerabilidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cat_vulnerabilidad_edit', { 'id': cat_Vulnerabilidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
