{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cat_amenaza</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_Amenaza.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cat_Amenaza.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cat_amenaza_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cat_amenaza_edit', { 'id': cat_Amenaza.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
