{% extends 'base.html.twig' %}

{% block body %}
<div class="container" style="margin-top:100px; margin-bottom:150px">
<div class="row well">
<div class="col-md-6">

     <div class="col-xs-12">
       <div class="panel panel-success">
        <div class="panel-heading">EDITAR CATEGORIA AMENAZA</div> 
        <div class="panel-body">
    

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br>
        <input class="btn" type="submit" value="Editar" />
         <a class="btn" href="{{ path('cat_amenaza_index') }}">Regresar</a>

         <br>
         <br>
         <ul>
        <li>
    {{ form_end(edit_form) }}
        </li>
    </ul>
     <a href="{{path ('cat_amenaza_delete', {'id':cat_Amenaza.id}) }}">Eliminar</a>
      </div>
    </div>
   </div>
 </div>
</div>
{% endblock %}