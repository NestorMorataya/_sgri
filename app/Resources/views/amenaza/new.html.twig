{% extends 'base.html.twig' %}

{% block body %}
    <div class="container" style="margin-top:30px;">
  <div class="row well">
   <div class="col-md-6">
       <div class="page-header">
            <h2>NUEVA AMENAZA</h2> 
       </div>

    {{ form_start(form) }}
     <div class="form-group">
       {{ form_label(form.nombre)}}
             {{ form_widget(form.nombre, {'attr':{'class':'form-control'}}) }}
        </div>
        <div class="form-group">
        {{ form_label(form.cat__amenaza)}}
              {{ form_widget(form.cat__amenaza) }}
         </div>

        <input class="btn" type="submit" value="Guardar" />
    {{ form_end(form) }}

    </div>
</div>
</div>

    <ul>
        <li>
            <a class="btn" href="{{ path('menu_amenaza') }}">Regresar</a>
        </li>
    </ul>
{% endblock %}
