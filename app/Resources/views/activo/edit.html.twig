{% extends 'base.html.twig' %}

{% block body %}
<div class="container" style="margin-top:15px; margin-bottom:180px;">
<hr>
<div class="col-xs-12">
<center>
    <h2>EDITAR ACTIVO</h2>


    {{ form_start(edit_form) }}
    <div class="form-group">
        {{ form_label(edit_form.codigo)}}
        {{ form_widget(edit_form.codigo) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.nombre)}}
        {{ form_widget(edit_form.nombre) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.descripcion)}}
        {{ form_widget(edit_form.descripcion) }}
    </div>
    <div class="form-group">
        <label>Unidad responsable</label>
        {{ form_widget(edit_form.unidadResponsable) }}
    </div>
    <div class="form-group">
        <label>Persona responsable</label>
        {{ form_widget(edit_form.personaResponsable) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.ubicacion)}}
        {{ form_widget(edit_form.ubicacion) }}
    </div>
    <div class="form-group">
        {{ form_label(edit_form.cantidad)}}
        {{ form_widget(edit_form.cantidad) }}
    </div>
        <input class="btn" type="submit" value="Editar" />
        <a class="btn" href="{{ path('activo_index') }}">Regresar</a>
    {{ form_end(edit_form) }}
    </div>
    </div>
</div>

 </div>
</center>
</div>
{% endblock %}
