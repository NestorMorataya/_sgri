{% extends 'base.html.twig' %}
{% block stylesheets %}
<link rel="stylesheet" type="text/css" href="{{ asset('public/css/tablas.css')}}">
{% endblock %}
{% block body %}
<h2>Plan tratamiento {{ plan.descripcion }}</h2>
<hr>
<br>
<br>
<br>
<div class="box-body table-responsive">
    <table id='tabla' class="table table-hover">
        <thead>
            <tr>
                
                <th>Control</th>
                  <th>Porcentaje Avance</th>
                <th>Acciones</th>
              
            </tr>
        </thead>
        <tbody>
        {% for proceso in procesos %}
            <tr>
                
                <td>{{ proceso.control}}</td>
                   <td>{{ proceso.procesoTarea}} %</td>
                <td><ul>

                        <li>
                            <a href="{{ path('tarea_mostrar', { 'id': proceso.id }) }}">Agregar Tarea</a>
                        </li>
                        <br>
                         <li>
                            <a href="{{ path('tarea_seguimiento', { 'id': proceso.id }) }}">Ver seguimiento</a>
                        </li>

                    </ul></td>
            </tr>
        {% endfor %}
         </tbody>
      </table>
    </div>

    
{% endblock %}



