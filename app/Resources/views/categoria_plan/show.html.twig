{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoria_plan</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria_Plan.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categoria_Plan.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoria_plan_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoria_plan_edit', { 'id': categoria_Plan.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
