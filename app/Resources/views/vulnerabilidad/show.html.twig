{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vulnerabilidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vulnerabilidad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ vulnerabilidad.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vulnerabilidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vulnerabilidad_edit', { 'id': vulnerabilidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
